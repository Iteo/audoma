name: Audoma test

on: [push]

jobs:
 audoma-test:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - name: run tests
      run: |
        echo "---> Building test image and running tests"
        cd docker && docker-compose run --rm tests
